#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<int> mxs;
    vector<int> mxsw;

    vector<int> mxe;
    vector<int> mxew;

    vector<vector<int>> conn;

    while(true) {
        for(int i = 0; i < conn.size(); ++i) {
            int mx_ln = 0;
            int mx_lnw = 0;
            for(int j = 0; j < conn[i].size(); ++j) {
                mx
                if(mx_ln < mxe[])
                mx_ln = max(mx_ln, mxe[conn[i][j]]);
            }
        }
    }

}
