#include <bits/stdc++.h>
using namespace std;

typedef long long int ll;

int main() {
	ll x0, y0, x1, y1;
	cin >> x0 >> y0 >> x1 >> y1;
	ll x, y;
	cin >> x >> y;

	cout << (x < x0) ? (x0 - x) : () << endl;
}
