(defparameter *n* 200005)
(defparameter mu (make-array n :initial-element 1))
(defparameter radical (make-array n :initial-element 1))
(defparameter squarefree #())

(defun init ()
  (loop for i from 0 to (- *n* 1) do (
                                      )))

(defparamete exists (make-array '(1000) :initial-element nil))
