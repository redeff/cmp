#include <bits/stdc++.h>
using namespace std;

struct Prob {
	ll les;
	ll mor;
}

int main() {
	int n, q;
	cin >> n >> q;
	vector<ll> ns(n);
	for(int i = 0; i < n; ++i) cin >> ns[i];
	vector<vector<Prob>> ps(n, vector<Prob>(n));

	for(int i = 0; i < q; ++i) {
		
	}
}
